{
  "name": "dallas-college-ai-club",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dallas College AI Club website, editorial pages, and browser games",
  "engines": {
    "node": ">=22.13"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "espree": "^11.2.0",
    "globals": "^17.12.0",
    "postcss": "^8.5.28",
    "prettier": "^3.9.6"
  },
  "scripts": {
    "start": "hugo server --bind 127.0.0.1 --port 4174 --disableFastRender",
    "build": "hugo --cleanDestinationDir --panicOnWarning",
    "test": "npm run build && node check.mjs --test",
    "validate": "npm run build && node check.mjs --validate",
    "lint": "eslint static tests database *.mjs eslint.config.js --max-warnings 0",
    "format": "prettier --write .github static tests docs data database *.mjs eslint.config.js package.json",
    "format:check": "prettier --check .github static tests docs data database *.mjs eslint.config.js package.json",
    "check": "npm run build && npm run format:check && npm run lint && node check.mjs"
  }
}
